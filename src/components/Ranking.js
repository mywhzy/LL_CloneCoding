import React from "react";
import styled from "styled-components";

const RakingBox = styled.div`
  background-color: black;
  color: white;
  font-size: 22px;
`;

const Ranking = () => {
  return <RakingBox></RakingBox>;
};

export default Ranking;
